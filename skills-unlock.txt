# Skills whose `disable-model-invocation: true` install.sh strips, so `ship` and
# other skills can invoke them.
#
# Upstream sets that flag to reserve a skill for the human. Only list a skill
# here when you want an agent to reach it as part of a flow. `npx skills update`
# restores the flag, so use ./bin/skills-update for updates.
#
# Deliberately NOT listed:
#   wait-what   a nudge that only means anything when a human says it
#   implement   superseded by ship
wayfinder
grill-with-docs
improve-codebase-architecture
